#!/bin/sh

set -e

root_dir=$(git rev-parse --show-toplevel)

printf 'Running pre-push checks...\n'

dotnet test "$root_dir/api"

cd "$root_dir"
corepack pnpm test
bash scripts/install-cli.sh

printf 'Pre-push checks passed.\n'
